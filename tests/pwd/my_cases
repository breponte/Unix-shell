./bin/pwd
./bin/pwd -P
./bin/pwd -L
rm -rf ./tmp/pt && mkdir -p ./tmp/pt/real && ln -s "$(./bin/pwd)/tmp/pt/real" ./tmp/pt/link; ( cd ./tmp/pt/link && printf '%s\n%s\n' "$(pwd -L)" "$(pwd -P)" ) && rm -rf ./tmp
